name: Test Documentation

on:
  schedule:
    - cron: '0 22 * * *'  # Daily at 10 PM UTC
  push:
    branches:
      - master
      - main
      - feature/daily-documentation-workflow
  workflow_dispatch:

jobs:
  # Job 1: Prepare for overnight processing
  prepare:
    name: 1️⃣ Prepare Overnight Run
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      
      - name: Install dependencies
        run: npm install
      
      - name: Prepare for overnight processing
        run: |
          echo "🌙 Preparing overnight test processing..."
          echo "📦 All 51 tests will be processed"
          echo "🤖 7 providers (1 per batch)"
          echo "⏱️  20 seconds between tests"
          echo "⏱️  Total estimated time: ~3 hours"
      
      - uses: actions/upload-artifact@v4
        with:
          name: ready
          path: package.json
          retention-days: 1

  # Job 2: Process with TOP providers only (100% success rate)
  analyze:
    name: 2️⃣ Phase 1 - Batch ${{ matrix.batch }}
    runs-on: ubuntu-latest
    needs: prepare
    strategy:
      matrix:
        batch: [0, 1]  # Only 2 batches - OR-Llama-70B and OR-Gemma-27B
      fail-fast: false
    
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      
      - run: npm ci
      
      - name: Process with proven provider (20s between tests)
        run: |
          PROVIDER_NAME=$([ "${{ matrix.batch }}" -eq 0 ] && echo "OR-Llama-70B (100%)" || echo "OR-Gemma-27B (100%)")
          echo "🎯 Batch ${{ matrix.batch }} - $PROVIDER_NAME"
          echo "⏱️  20 second delays between tests"
          node auto_documentator/scripts/overnight-processor.js ${{ matrix.batch }}
        timeout-minutes: 120  # 2 hours max per batch (51 tests / 2 batches)
        env:
          AI_API_KEY: ${{ secrets.AI_API_KEY }}
          AI_API_KEY_2: ${{ secrets.AI_API_KEY_2 }}
          AI_API_KEY_5: ${{ secrets.AI_API_KEY_5 }}
          AI_API_KEY_6: ${{ secrets.AI_API_KEY_6 }}
          AI_API_KEY_7: ${{ secrets.AI_API_KEY_7 }}
      
      - uses: actions/upload-artifact@v4
        with:
          name: batch-docs-${{ matrix.batch }}
          path: documentation/batch-${{ matrix.batch }}.json
          retention-days: 7

  # Job 3: Analyze first phase results
  analyze-results:
    name: 3️⃣ Analyze Phase 1 Results
    runs-on: ubuntu-latest
    needs: analyze
    if: always()
    outputs:
      failed-count: ${{ steps.analyze.outputs.failed-count }}
      working-batches: ${{ steps.analyze.outputs.working-batches }}
      needs-retry: ${{ steps.analyze.outputs.needs-retry }}
    
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      
      - run: npm ci
      
      - uses: actions/download-artifact@v4
        with:
          pattern: batch-docs-*
          path: documentation/
          merge-multiple: true
      
      - name: Identify failures and working providers
        id: analyze
        run: |
          OUTPUT=$(node auto_documentator/scripts/identify-failures.js | tee analysis.txt)
          echo "$OUTPUT"
          
          FAILED=$(grep "failed-count=" analysis.txt | cut -d= -f2 || echo "0")
          WORKING=$(grep "working-batches=" analysis.txt | cut -d= -f2 || echo "")
          
          echo "failed-count=$FAILED" >> $GITHUB_OUTPUT
          echo "working-batches=$WORKING" >> $GITHUB_OUTPUT
          
          if [ "$FAILED" -gt 0 ] && [ -n "$WORKING" ]; then
            echo "needs-retry=true" >> $GITHUB_OUTPUT
            echo "🔄 Will retry $FAILED tests with working providers: $WORKING"
          else
            echo "needs-retry=false" >> $GITHUB_OUTPUT
            echo "✅ No retry needed"
          fi
      
      - uses: actions/upload-artifact@v4
        if: steps.analyze.outputs.needs-retry == 'true'
        with:
          name: retry-data
          path: |
            documentation/failed-tests.txt
            documentation/batch-stats.json
          retention-days: 1

  # Job 4: Retry failed tests with working providers (1 minute delays)
  retry:
    name: 4️⃣ Retry with Batch ${{ matrix.working-batch }}
    runs-on: ubuntu-latest
    needs: analyze-results
    if: needs.analyze-results.outputs.needs-retry == 'true'
    strategy:
      matrix:
        working-batch: ${{ fromJson(format('[{0}]', needs.analyze-results.outputs.working-batches)) }}
      fail-fast: false
    
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      
      - run: npm ci
      
      - uses: actions/download-artifact@v4
        with:
          name: retry-data
          path: documentation/
      
      - name: Retry failed tests (1 minute delays - STRICT)
        run: |
          echo "🔄 Retrying with working provider from Batch ${{ matrix.working-batch }}"
          echo "⏱️  STRICT 1 MINUTE between tests"
          node auto_documentator/scripts/retry-with-working-providers.js ${{ matrix.working-batch }}
        timeout-minutes: 360
        env:
          AI_API_KEY: ${{ secrets.AI_API_KEY }}
          AI_API_KEY_2: ${{ secrets.AI_API_KEY_2 }}
          AI_API_KEY_5: ${{ secrets.AI_API_KEY_5 }}
          AI_API_KEY_6: ${{ secrets.AI_API_KEY_6 }}
          AI_API_KEY_7: ${{ secrets.AI_API_KEY_7 }}
      
      - uses: actions/upload-artifact@v4
        with:
          name: retry-docs-${{ matrix.working-batch }}
          path: documentation/batch-retry-${{ matrix.working-batch }}.json
          retention-days: 7

  # Job 5: Final consolidation
  consolidate:
    name: 5️⃣ Final Consolidation
    runs-on: ubuntu-latest
    needs: [analyze, analyze-results, retry]
    if: always()
    outputs:
      total-entries: ${{ steps.consolidate.outputs.total }}
    
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      
      - run: npm ci
      
      - uses: actions/download-artifact@v4
        with:
          pattern: batch-docs-*
          path: documentation/
          merge-multiple: true
      
      - uses: actions/download-artifact@v4
        continue-on-error: true
        with:
          pattern: retry-docs-*
          path: documentation/
          merge-multiple: true
      
      - name: Merge all documentation
        id: consolidate
        run: |
          OUTPUT=$(node auto_documentator/scripts/merge-documentation.js 2>&1)
          echo "$OUTPUT"
          
          TOTAL=$(echo "$OUTPUT" | grep -oP 'TOTAL_ENTRIES:\K\d+' || echo "0")
          echo "total=$TOTAL" >> $GITHUB_OUTPUT
          echo "✅ Final documentation: $TOTAL entries"
        env:
          GITHUB_ACTOR: ${{ github.actor }}
          GIT_USER_NAME: ${{ github.event.pusher.name || github.actor }}
      
      - name: Create simple header
        run: |
          echo "All batches processed - merging results"
      
      - uses: actions/upload-artifact@v4
        with:
          name: consolidated-docs
          path: documentation/CONSOLIDATED_DOCUMENTATION.md
          retention-days: 7
      
      - uses: actions/upload-artifact@v4
        with:
          name: existing-docs
          path: documentation/EXISTING_DOCUMENTATION.md
          retention-days: 365  # Keep for incremental updates
      
      - uses: actions/upload-artifact@v4
        with:
          name: test-metadata
          path: documentation/test-metadata.json
          retention-days: 365  # Keep metadata for a year

  # Job 6: Update Coda (parallel with Notion)
  update-coda:
    name: 6️⃣ A Update Coda
    runs-on: ubuntu-latest
    needs: consolidate
    
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      
      - run: npm ci
      
      - uses: actions/download-artifact@v4
        with:
          name: consolidated-docs
          path: documentation/
      
      - name: Update Coda page
        run: node auto_documentator/scripts/update-coda-page.js
        env:
          CODA_API_TOKEN: ${{ secrets.CODA_API_TOKEN }}
          CODA_DOC_ID: ${{ secrets.CODA_DOC_ID || 'dza2s1eOIhA' }}
          CODA_PAGE_ID: ${{ secrets.CODA_PAGE_ID || 'suLCLolD' }}

  # Job 6B: Update Notion (parallel with Coda)
  update-notion:
    name: 6️⃣ B Update Notion
    runs-on: ubuntu-latest
    needs: consolidate
    
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      
      - run: npm ci
      
      - uses: actions/download-artifact@v4
        with:
          name: consolidated-docs
          path: documentation/
      
      - name: Update Notion page
        run: node auto_documentator/scripts/update-notion-page.js
        env:
          NOTION_SECRET: ${{ secrets.NOTION_SECRET }}
          NOTION_PAGE_ID: '2bdd92a99cff809e98ddcec14e6b7bdc'

  # Job 7: Send notification
  notify:
    name: 7️⃣ Notify
    runs-on: ubuntu-latest
    needs: [consolidate, update-coda, update-notion]
    if: always()
    
    steps:
      - name: Send Slack notification
        run: |
          CODA_STATUS="${{ needs.update-coda.result }}"
          NOTION_STATUS="${{ needs.update-notion.result }}"
          
          if [ "$CODA_STATUS" == "success" ] && [ "$NOTION_STATUS" == "success" ]; then
            EMOJI="✅"
            STATUS="Success"
          elif [ "$CODA_STATUS" == "success" ] || [ "$NOTION_STATUS" == "success" ]; then
            EMOJI="⚠️"
            STATUS="Partial Success"
          else
            EMOJI="❌"
            STATUS="Failed"
          fi
          
          SUMMARY="*Tests:* ${{ needs.prepare.outputs.total-tests }}\n"
          SUMMARY="${SUMMARY}*Entries:* ${{ needs.consolidate.outputs.total-entries }}\n"
          SUMMARY="${SUMMARY}*Coda:* $([ "$CODA_STATUS" == "success" ] && echo "✅" || echo "❌")\n"
          SUMMARY="${SUMMARY}*Notion:* $([ "$NOTION_STATUS" == "success" ] && echo "✅" || echo "❌")\n"
          SUMMARY="${SUMMARY}\n*Links:*\n"
          SUMMARY="${SUMMARY}• <https://coda.io/d/dza2s1eOIhA/_suLCLolD|Coda Docs>\n"
          SUMMARY="${SUMMARY}• <https://www.notion.so/2bdd92a99cff809e98ddcec14e6b7bdc|Notion Docs>"
          
          curl -X POST -H 'Content-type: application/json' --data '{
            "blocks": [
              {
                "type": "header",
                "text": {"type": "plain_text", "text": "'"${EMOJI} Test Documentation - ${STATUS}"'"}
              },
              {
                "type": "section",
                "text": {"type": "mrkdwn", "text": "'"${SUMMARY}"'"}
              },
              {
                "type": "context",
                "elements": [{"type": "mrkdwn", "text": "<${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}|View Workflow>"}]
              }
            ]
          }' "${{ secrets.SLACK_WEBHOOK_URL }}"
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}

